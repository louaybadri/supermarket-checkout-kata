spring.application.name=checkout

# The catalog and this week's offers live in their own file.
spring.config.import=classpath:catalog.yml

# In-memory H2: the schema and the seed are rebuilt on every start.
spring.datasource.url=jdbc:h2:mem:checkout
spring.jpa.hibernate.ddl-auto=create-drop
